name: Copyright Headers

on:
  pull_request:
    branches: [ main, 'maintenance/*' ]
    types: [opened, synchronize, reopened]
  push:
    branches: [ main, 'maintenance/*' ]

permissions:
  contents: write

concurrency:
  group: copyright-headers-${{ github.ref }}
  cancel-in-progress: true

jobs:
  format-headers:
    name: Format Copyright Headers
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          ref: ${{ github.head_ref }}

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven

      - name: Check copyright headers
        if: github.event_name == 'push'
        run: mvn -B license:check

      - name: Format copyright headers
        if: github.event_name == 'pull_request'
        run: mvn -B license:format

      - name: Check for changes
        if: github.event_name == 'pull_request'
        id: check
        run: |
          if ! git diff --quiet; then
            echo "changes=true" >> $GITHUB_OUTPUT
          fi

      - name: Commit and push changes
        if: github.event_name == 'pull_request' && steps.check.outputs.changes == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          git commit -m "chore: Format copyright headers"
          git push
